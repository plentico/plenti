package build

import "fmt"

// Static builds the html fallbacks.
func Static(nodeList string) {

	fmt.Println("\nPreparing static HTML for svelte/register")
	//for i, node := range nodeList {
	//fmt.Printf("Node %d is: %s\n", i, node)
	//fmt.Printf("Node type is: %s\n", node.nodeType)
	//fmt.Printf("Node path is: %s\n", node.nodePath)
	//}
}
